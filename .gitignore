Nothing needs to be added to .gitignore since the only change is adding a single HTML file (`lords_of_realm_2.html`), which is a source/config file and not a build artifact, dependency, or temporary file.